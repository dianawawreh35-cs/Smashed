# features

One folder per supervisor-facing feature (hooks, queries and feature-local
components), added alongside the API endpoints it consumes: `auth`, `users`,
`contacts`, `communications`, `classifications`, `tasks`, `reports`,
`settings`. Cross-feature UI lives in `src/components`, routed screens in
`src/pages`.
